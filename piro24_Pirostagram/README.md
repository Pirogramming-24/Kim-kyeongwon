# 📸 Pirostagram

Django 기반 인스타그램 클론 프로젝트

## 📋 프로젝트 소개

Pirostagram은 Django와 JavaScript를 활용하여 인스타그램의 핵심 기능을 구현한 소셜 미디어 플랫폼입니다.

## ✅ 구현된 기능

### 필수 구현 기능 (7가지)

#### 1. 📙 게시글 기능
- 이미지와 텍스트를 포함한 게시글 작성
- 게시글 수정 및 삭제 (작성자만 가능)
- 게시글 상세 페이지
- 피드에서 팔로우한 사람들의 게시글 표시

#### 2. 👍 좋아요 기능
- **Ajax를 활용한 실시간 좋아요 토글** (페이지 새로고침 없음)
- 하트 아이콘 색상 변경 (회색 ↔ 빨강)
- 좋아요 수 실시간 업데이트
- 게시글별 좋아요 수 표시

#### 3. 💬 댓글 기능
- 댓글 작성, 수정, 삭제
- 게시글별 댓글 목록 표시
- 게시 버튼을 통한 댓글 작성
- 댓글 수 표시

#### 4. 📜 스토리 기능
- 한 장씩 스토리 업로드
- 여러 스토리 연속 재생
- **5초마다 자동으로 다음 스토리로 전환**
- 진행 바 애니메이션
- 이전/다음 버튼으로 수동 조작
- 키보드 방향키 지원 (←, →, ESC)
- **24시간 후 자동 만료**
- 스토리 아바타에 그라데이션 링 표시

#### 5. 🔍 유저 검색 기능
- 사용자 이름(username)으로 검색
- 이름(first_name, last_name)으로 검색
- 검색 결과에서 바로 팔로우 가능
- 프로필 페이지로 이동

#### 6. 👬 팔로우 기능
- **Ajax를 활용한 실시간 팔로우/언팔로우** (페이지 새로고침 없음)
- 팔로우/팔로잉 버튼 상태 실시간 변경
- 팔로워 수 실시간 업데이트
- 팔로워/팔로잉 목록 조회
- 팔로우한 사람들의 게시글만 피드에 표시

#### 7. 🐳 Docker 배포
- Dockerfile 작성
- .dockerignore 설정
- Docker Hub 업로드 완료
- 원클릭 실행 가능

### 추가 구현 기능

#### 🎯 게시글 정렬
- **최신순**: 가장 최근 게시물부터 표시 (기본값)
- **좋아요순**: 좋아요가 많은 게시물부터 표시

#### 🎨 인스타그램 스타일 UI
- Bootstrap 5 기반 반응형 디자인
- 사이드바 네비게이션
- 인스타그램과 유사한 색상 및 레이아웃
- Font Awesome 아이콘
- 깔끔한 카드 디자인

## 🚀 실행 방법

### 방법 1: 로컬 환경에서 실행

#### 1. 저장소 클론
```bash
git clone <repository-url>
cd piro24_Pirostagram
```

#### 2. 가상환경 생성 및 활성화
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Mac/Linux
python3 -m venv venv
source venv/bin/activate
```

#### 3. 패키지 설치
```bash
pip install -r requirements.txt
```

#### 4. 데이터베이스 마이그레이션
```bash
python manage.py makemigrations
python manage.py migrate
```

#### 5. 관리자 계정 생성
```bash
python manage.py createsuperuser
```
- Username: 원하는 이름 입력
- Email: 이메일 입력
- Password: 비밀번호 입력

#### 6. 서버 실행
```bash
python manage.py runserver
```

#### 7. 브라우저에서 접속
```
http://localhost:8000
```

### 방법 2: Docker로 실행 (권장)

#### Docker Hub에서 이미지 다운로드 및 실행
```bash
docker pull kkw610/pirostagram:latest
docker run -d -p 8000:8000 kkw610/pirostagram:latest
```

#### 브라우저에서 접속
```
http://localhost:8000
```

### 방법 3: Docker 이미지 직접 빌드

#### 1. 저장소 클론
```bash
git clone <repository-url>
cd piro24_Pirostagram
```

#### 2. Docker 이미지 빌드
```bash
docker build -t pirostagram:latest .
```

#### 3. 컨테이너 실행
```bash
docker run -d -p 8000:8000 --name pirostagram_app pirostagram:latest
```

#### 4. 브라우저에서 접속
```
http://localhost:8000
```

## 🐳 Docker 정보

**Docker Hub 이미지:**
```
kkw610/pirostagram:latest
```

**Docker Hub 링크:**
```
https://hub.docker.com/r/kkw610/pirostagram
```

## 📂 프로젝트 구조

```
pirostagram/
├── config/                  # 프로젝트 설정
│   ├── settings.py         # Django 설정
│   ├── urls.py             # 메인 URL 라우팅
│   ├── wsgi.py
│   └── asgi.py
├── users/                   # 유저 앱
│   ├── models.py           # User 모델 (프로필, 팔로우)
│   ├── views.py            # 인증, 프로필, 검색, 팔로우 뷰
│   ├── forms.py            # 회원가입, 로그인, 프로필 수정 폼
│   ├── urls.py
│   └── admin.py
├── posts/                   # 게시글 앱
│   ├── models.py           # Post, Comment 모델
│   ├── views.py            # 게시글 CRUD, 좋아요(Ajax), 댓글
│   ├── forms.py
│   ├── urls.py
│   └── admin.py
├── stories/                 # 스토리 앱
│   ├── models.py           # Story, StoryImage 모델
│   ├── views.py            # 스토리 생성/조회
│   ├── forms.py
│   ├── urls.py
│   └── admin.py
├── templates/               # HTML 템플릿
│   ├── base.html
│   ├── users/
│   ├── posts/
│   └── stories/
├── static/                  # 정적 파일
├── media/                   # 업로드 파일
├── manage.py
├── requirements.txt
├── Dockerfile
├── .dockerignore
└── .gitignore
```

## 🔑 주요 기능 상세

### Ajax 좋아요 구현
```javascript
// templates/posts/feed.html
$('.like-btn').click(function() {
    $.ajax({
        url: '/posts/' + postId + '/like/',
        headers: {'X-Requested-With': 'XMLHttpRequest'},
        success: function(data) {
            // 아이콘 색상 변경
            if (data.is_liked) {
                icon.removeClass('far').addClass('fas');
            } else {
                icon.removeClass('fas').addClass('far');
            }
            // 좋아요 수 업데이트
            likesCountElement.text(data.likes_count);
        }
    });
});
```

### Ajax 팔로우 구현
```javascript
// templates/users/profile.html
$('.follow-btn').click(function() {
    $.ajax({
        url: '/users/' + username + '/follow/',
        headers: {'X-Requested-With': 'XMLHttpRequest'},
        success: function(data) {
            // 버튼 텍스트 변경
            btn.text(data.is_following ? '팔로잉' : '팔로우');
            // 팔로워 수 업데이트
            $('.stat-number').text(data.followers_count);
        }
    });
});
```

### 스토리 자동재생 구현
```javascript
// templates/stories/story_view.html
function changeImage(index) {
    imageElement.attr('src', images[index]);
    
    // 진행 바 애니메이션
    progressBars.eq(index).addClass('active');
    
    // 5초 후 다음 스토리
    setTimeout(function() {
        changeImage(index + 1);
    }, 5000);
}
```

## 🎯 사용 시나리오

### 1. 회원가입 및 로그인
1. 회원가입 페이지에서 계정 생성
2. 로그인

### 2. 프로필 설정
1. 사이드바에서 "프로필" 클릭
2. "프로필 수정하기" 클릭
3. 프로필 이미지, 자기소개 작성

### 3. 유저 팔로우
1. 사이드바에서 "유저 검색" 클릭
2. 사용자 이름 검색
3. "팔로우" 버튼 클릭

### 4. 게시글 작성
1. 사이드바에서 "게시글 만들기" 클릭
2. 이미지 선택 및 내용 작성
3. "작성" 버튼 클릭

### 5. 좋아요 및 댓글
1. 피드에서 하트 아이콘 클릭 (좋아요)
2. 댓글 입력 후 "게시" 버튼 클릭

### 6. 스토리 업로드
1. 사이드바에서 "스토리 만들기" 클릭
2. 이미지 선택 후 업로드
3. 여러 개 올리려면 반복

### 7. 스토리 보기
1. 피드 상단의 스토리 아바타 클릭
2. 5초마다 자동으로 다음 스토리로 전환
3. 좌우 클릭 또는 방향키로 수동 조작

## 🛠️ 유용한 명령어

### Django 명령어
```bash
# 마이그레이션 생성
python manage.py makemigrations

# 마이그레이션 적용
python manage.py migrate

# 관리자 계정 생성
python manage.py createsuperuser

# 서버 실행
python manage.py runserver

# Django Shell
python manage.py shell
```

### Docker 명령어
```bash
# 컨테이너 확인
docker ps

# 컨테이너 로그 확인
docker logs pirostagram_app

# 컨테이너 중지
docker stop pirostagram_app

# 컨테이너 시작
docker start pirostagram_app

# 컨테이너 삭제
docker rm pirostagram_app

# 이미지 확인
docker images

# 이미지 삭제
docker rmi kkw610/pirostagram:latest
```

## 🔧 문제 해결

### 포트 충돌 시
```bash
# 다른 포트로 실행 (예: 8080)
docker run -d -p 8080:8000 kkw610/pirostagram:latest
```

### 데이터베이스 초기화
```bash
# db.sqlite3 삭제 후
python manage.py makemigrations
python manage.py migrate
```

### 미디어 파일 문제
```bash
# media 디렉토리가 없다면
mkdir -p media/posts media/stories media/profile_pics
```

## 📝 요구사항 체크리스트

- [x] 📙 게시글 기능 (작성, 수정, 삭제)
- [x] 👍 좋아요 기능 (Ajax 토글, 실시간 업데이트)
- [x] 💬 댓글 기능 (작성, 수정, 삭제)
- [x] 📜 스토리 기능 (24시간 만료, 자동 재생)
- [x] 🔍 유저 검색 기능
- [x] 👬 팔로우 기능 (Ajax 토글)
- [x] 🐳 Docker 배포

### 챌린지 과제
- [x] 👍 Ajax 좋아요
- [x] 🎨 인스타 UI 유사화
- [x] 🖼️ 스토리 자동으로 넘기기 (5초)
- [x] 🗂️ 게시글 정렬 (최신순, 좋아요순)